{
  "name": "@echo/shared",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "Echo messenger 'shared build'",
    "clean": "Echo messenger 'shared clean'"
  },
  "dependencies": {
    "zod": "^4.1.0"
  }
}