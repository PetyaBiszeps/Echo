{
  "name": "@echo/shared",
  "version": "1.0.0",
  "scripts": {
    "dev": "Echo messenger 'shared dev'",
    "build": "Echo messenger 'shared build'"
  },
  "dependencies": {
    "zod": "^4.1.0"
  }
}