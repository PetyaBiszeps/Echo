{
  "name": "@echo/client",
  "version": "1.0.0",
  "scripts": {
    "dev": "Echo messenger 'client dev'",
    "build": "Echo messenger 'client build'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "vue": "^3.5.20"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
