{
  "name": "@echo/server",
  "version": "1.0.0",
  "scripts": {
    "dev": "Echo messenger 'server dev'",
    "build": "Echo messenger 'client build'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "typescript": "^5.9.2"
  }
}
