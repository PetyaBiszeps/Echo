// import { spawn } from 'node:child_process'

    // Constants
const commands = [{
    name: '',
    cmd: ''
}, {
    name: '',
    cmd: ''
}, {
    name: '',
    cmd: ''
}]
// const children = []

    // Init
commands.forEach(({ name, cmd }) => {
    console.log(`${name}: ${cmd}`)
})