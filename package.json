{
  "name": "echo-messenger",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @echo/server dev & yarn workspace @echo/client dev",
    "dev:client": "yarn workspace @echo/client dev",
    "dev:server": "yarn workspace @echo/server dev",
    "build": "Echo messenger 'yarn workspaces run build...'"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}